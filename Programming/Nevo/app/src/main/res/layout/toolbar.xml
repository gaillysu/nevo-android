<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              xmlns:app="http://schemas.android.com/apk/res-auto"
              android:layout_width="match_parent"
              android:layout_height="wrap_content"
              android:orientation="vertical"
    >

    <android.support.v7.widget.Toolbar
        android:id="@+id/main_toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/tool_bar_bg_color"
        android:elevation="0dp"
        android:gravity="center_horizontal"
        android:theme="@style/NevoTheme"
        app:popupTheme="@style/NevoActionBar"
        app:theme="@style/NevoActionBar"
        app:titleTextColor="@color/toolbar_center_date_text_color">


        <LinearLayout
            android:id="@+id/lunar_tool_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="horizontal"
            >

            <ImageView
                android:id="@+id/lunar_tool_bar_title_date_icon"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:src="@drawable/date_icon"
                android:visibility="gone"/>

            <TextView
                android:id="@+id/lunar_tool_bar_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginLeft="3dp"
                android:textColor="@color/toolbar_center_date_text_color"
                android:textSize="22sp"
                />
        </LinearLayout>
    </android.support.v7.widget.Toolbar>

    <View
        android:id="@+id/toolbar_line_view"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@android:color/white"
        />

</LinearLayout>
